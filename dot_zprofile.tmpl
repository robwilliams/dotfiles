# Homebrew (Apple Silicon)
{{ if eq .chezmoi.os "darwin" }}
eval "$(/opt/homebrew/bin/brew shellenv)"
{{ end }}

# Bun (must come after brew to take precedence)
[ -d "$HOME/.bun/bin" ] && export PATH="$HOME/.bun/bin:$PATH"