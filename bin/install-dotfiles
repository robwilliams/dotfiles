#!/bin/bash -x
cd ~/dotfiles

for file in tmux.conf zsh zshrc gitignore_global vim gvimrc vimrc ssh/config gitconfig gemrc irbrc ackrc railsrc vimrc.bundles
do
  echo "Linking $file to ~/.$file..";
  rm ~/.$file;
  ln -sf ~/dotfiles/$file ~/.$file;
done
